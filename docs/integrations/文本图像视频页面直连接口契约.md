# 文本、图像与视频页面直连接口契约

## 1. 目的

本文定义模型网关需要实现的服务端接口，使目标客户端无需修改代码即可在管理员页面选择 `OpenAI` 协议，填写 Base URL 和 API Key，同步模型后使用文本、图像与视频能力

这是一份目标客户端消费契约，不是完整的 OpenAI API 规范。未在本文列出的 OpenAI 接口不属于本次直连要求

## 2. 接入结果

完成本契约后，管理员只需要执行以下操作

1. 进入“管理员后台 -> 上游配置 -> 模型渠道”
2. 选择 `OpenAI` 协议
3. 填写 LiteLLM Base URL，例如 `https://gateway.example.com` 或 `https://gateway.example.com/v1`
4. 填写 LiteLLM API Key
5. 点击“同步模型”
6. 确认文本、图像、视频模型及其能力
7. 点击“同步并自动合并”，按需设为默认模型
8. 启用渠道并保存

Base URL 必须能被目标客户端的服务端访问。浏览器能访问不代表服务端或容器能访问。若 LiteLLM 使用私网地址，部署者还需要使用目标客户端已有的私网上游白名单配置，这不需要修改业务代码

## 3. 按能力实现的接口

| 能力 | 方法 | 路径 | 必须性 |
| --- | --- | --- | --- |
| 模型目录 | GET | `/v1/models` | 必须 |
| 文本生成 | POST | `/v1/chat/completions` | 必须 |
| 图像生成 | POST | `/v1/images/generations` | 必须 |
| 图像编辑 | POST | `/v1/images/edits` | 必须 |
| 视频创建 | POST | `/v1/videos` | 必须 |
| 视频状态 | GET | `/v1/videos/{video_id}` | 必须 |
| 视频内容 | GET | `/v1/videos/{video_id}/content` | 使用网关媒体代理时必须 |
| 视频内容元信息 | HEAD | `/v1/videos/{video_id}/content` | 推荐 |

当前接入不要求 `/v1/responses`、异步图像任务、音频接口或视频取消接口

`/v1/models` 可以只发布部分能力。目录发布了 text、image 或 video 中的哪一类，网关就必须实现该类对应接口。若目标是一个渠道同时覆盖文本、图像和视频全部页面，才需要三类接口和模型全部通过

## 4. 通用约定

### 4.1 鉴权

所有接口使用同一个 Bearer Token

```http
Authorization: Bearer <API_KEY>
```

API Key 由管理员页面保存，目标客户端只在服务端使用，不会交给普通浏览器用户

### 4.2 Content-Type

JSON 请求使用

```http
Content-Type: application/json; charset=utf-8
```

文件请求使用真实 multipart boundary

```http
Content-Type: multipart/form-data; boundary=...
```

服务端不能要求调用方手工写死 multipart boundary

### 4.3 错误

失败必须使用非 2xx HTTP 状态并返回 JSON

```json
{
  "error": {
    "message": "具体且可公开给用户的错误原因",
    "type": "invalid_request_error",
    "code": "invalid_parameter"
  }
}
```

不得以 HTTP 200 返回错误页、纯文本错误、空对象或缺少任务 ID 的半成功结果

推荐状态码

| 状态码 | 语义 |
| --- | --- |
| 400 | 参数错误 |
| 401 | API Key 无效 |
| 403 | 无权限调用模型 |
| 404 | 模型或任务不存在 |
| 409 | 同一幂等键对应了不同请求 |
| 413 | 上传文件过大 |
| 415 | Content-Type 不支持 |
| 422 | 参数语义错误 |
| 429 | 限流或并发额度不足 |
| 500/502/503 | 网关或上游暂时失败 |

### 4.4 路径和重定向

Base URL 填写站点根地址或以 `/v1` 结尾均可，网关必须实际提供表格中的 `/v1/...` 路径

如果实现视频 content 接口，它必须直接返回媒体字节，不能向目标客户端返回 3xx。LiteLLM 可以在内部跟随供应商或 CDN 重定向，但最终对目标客户端必须是 200 或 206

## 5. 模型目录

### 5.1 请求

```http
GET /v1/models
Authorization: Bearer <API_KEY>
Accept: application/json
```

### 5.2 推荐响应

```json
{
  "object": "list",
  "data": [
    {
      "id": "gateway-text-pro",
      "object": "model",
      "capability": "text"
    },
    {
      "id": "gateway-image-pro",
      "object": "model",
      "capability": "image"
    },
    {
      "id": "gateway-video-pro",
      "object": "model",
      "capability": "video"
    }
  ],
  "has_more": false
}
```

每项必须包含稳定且可用于后续请求的 `id`。推荐显式返回 `capability`，允许值只有 `text`、`image`、`video`、`audio`

LiteLLM 当前常见的 `mode` 字段不能单独替代 `capability`，因为目标客户端不会读取 `mode` 来判断能力。目标客户端也能读取 `type`、`task`、`category`、`kind`、`endpoint`、`route`、`path`、`capabilities`、`output_modalities` 等提示，但新接口应统一使用明确的 `capability`

若暂时不能增加任何受支持的能力字段，模型 ID 必须包含明确的 `image` 或 `video` 关键词，未识别的 ID 会被当成文本模型

文本、图像和视频必须使用不同的公开模型 ID。LiteLLM 内部可以让同一个公开 ID 路由到多个部署，但不能让同一个公开 ID 同时代表不同能力

目录应该只返回当前 API Key 可以调用的生成模型。Embedding、reranker、OCR、ASR、STT、transcription、moderation 和安全分类模型不应作为本次目录结果

### 5.3 可选分页

```json
{
  "data": [
    {"id": "gateway-text-pro", "capability": "text"}
  ],
  "has_more": true,
  "last_id": "gateway-text-pro"
}
```

下一页请求为

```http
GET /v1/models?after=gateway-text-pro
```

如果模型数量不大，直接返回完整列表更简单

## 6. 文本生成

### 6.1 普通请求

```http
POST /v1/chat/completions
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

```json
{
  "model": "gateway-text-pro",
  "messages": [
    {"role": "system", "content": "可选系统指令"},
    {"role": "user", "content": "用户输入"},
    {"role": "assistant", "content": "可选历史回复"}
  ]
}
```

`messages[].content` 必须支持字符串。推荐同时透传 OpenAI 多模态数组，例如 `text` 和 `image_url` 内容块

### 6.2 普通响应

```json
{
  "id": "chatcmpl_123",
  "object": "chat.completion",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "最终文本"
      },
      "finish_reason": "stop"
    }
  ]
}
```

最低要求是 `choices[].message.content` 可解析。`content` 可以是字符串，也可以是包含文本内容块的数组

### 6.3 结构化 JSON

目标客户端会发送

```json
{
  "model": "gateway-text-pro",
  "messages": [
    {"role": "system", "content": "只返回严格 JSON 对象"},
    {"role": "user", "content": "生成结构化计划"}
  ],
  "response_format": {"type": "json_object"}
}
```

网关必须把 `response_format` 传给支持它的模型，或在网关侧保证最终 `message.content` 是合法 JSON 对象字符串

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "{\"tasks\":[]}"
      }
    }
  ]
}
```

结构化 JSON 用于统一创作规划、图像和视频参数规划、Canvas 及短剧分析，普通聊天成功但结构化输出不稳定仍然视为不兼容

### 6.4 Function Calling

目标客户端可能发送标准 `tools` 和 `tool_choice`

```json
{
  "model": "gateway-text-pro",
  "messages": [{"role": "user", "content": "规划任务"}],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "create_agent_plan",
        "description": "创建执行计划",
        "parameters": {"type": "object", "properties": {}}
      }
    }
  ],
  "tool_choice": {
    "type": "function",
    "function": {"name": "create_agent_plan"}
  }
}
```

兼容响应

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": null,
        "tool_calls": [
          {
            "id": "call_123",
            "type": "function",
            "function": {
              "name": "create_agent_plan",
              "arguments": "{\"tasks\":[]}"
            }
          }
        ]
      }
    }
  ]
}
```

若底层模型不支持工具调用，网关可以回退为 `message.content` 中的合法 JSON

### 6.5 流式响应

完整兼容应支持 `stream: true` 并返回标准 SSE

```http
Content-Type: text/event-stream
Cache-Control: no-cache
```

```text
data: {"choices":[{"index":0,"delta":{"role":"assistant","content":"部"}}]}

data: {"choices":[{"index":0,"delta":{"content":"分"}}]}

data: [DONE]
```

工具调用流需要累计返回 `delta.tool_calls[].function.arguments`

如果暂不实现流式，必须对 `stream: true` 明确返回 400、404、405、415、422 或 501，目标客户端才会安全回退为非流请求。不得返回 200 后输出损坏的 SSE

## 7. 图像生成

### 7.1 请求

```http
POST /v1/images/generations
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

```json
{
  "model": "gateway-image-pro",
  "prompt": "最终上游执行提示词",
  "n": 1,
  "quality": "high",
  "size": "1024x1024"
}
```

字段约定

| 字段 | 类型 | 约定 |
| --- | --- | --- |
| `model` | string | 必须，使用 `/v1/models` 返回的图像模型 ID |
| `prompt` | string | 必须 |
| `n` | integer | 可选，普通任务通常为 1 |
| `quality` | string | 可选，可能为 `low`、`medium`、`high`、`standard`、`hd` |
| `size` | string | 可选，精确 `WIDTHxHEIGHT` |

服务端必须接受未发送 `n`、`quality` 或 `size` 的请求。不得自行把缺失值改成与模型能力不符的固定参数

### 7.2 响应

推荐返回可下载 URL

```json
{
  "created": 1780000000,
  "data": [
    {"url": "https://cdn.example.com/generated/a.png"},
    {"url": "https://cdn.example.com/generated/b.png"}
  ]
}
```

也可以返回 Base64

```json
{
  "data": [
    {"b64_json": "iVBORw0KGgoAAA..."}
  ]
}
```

必须保留并返回所有成功结果，不能只转发第一张。数组第一项作为兼容主结果，其余项仍会被目标客户端保存

URL 必须能被目标客户端服务端下载。可以使用公网 URL、短期签名 URL或 LiteLLM 自身的媒体代理 URL。不能返回只在供应商容器内部可见的 localhost 地址

为了让管理员只选择内置 OpenAI 协议，图像生成应同步返回最终 `data[]`。如果供应商是异步接口，LiteLLM 应在内部完成提交、轮询和结果归一化

## 8. 图像编辑

若图像模型需要覆盖参考图工作台、统一创作、Canvas 编辑与分层，本接口必须实现。只支持纯文生图的模型可以不实现 edits，但不得把它当作完整参考图模型发布给这些入口

### 8.1 请求

```http
POST /v1/images/edits
Authorization: Bearer <API_KEY>
Content-Type: multipart/form-data; boundary=...
```

```text
model=gateway-image-pro
prompt=保持主体，只把背景改为夜景
n=1
aspect_ratio=16:9
resolution=2K
image=@reference-1.png
image=@reference-2.png
mask=@mask.png
background=transparent
```

字段约定

| 字段 | 类型 | 约定 |
| --- | --- | --- |
| `model` | text part | 必须 |
| `prompt` | text part | 必须 |
| `image` | repeated file part | 至少一份，必须接受多个同名 `image` |
| `mask` | file part | 可选 |
| `n` | text part | 可选 |
| `aspect_ratio` | text part | 可选，普通画面比例，例如 `16:9`；自动时省略 |
| `resolution` | text part | 可选，`1K`、`2K`、`4K` 等模型声明的 K 档；自动时省略 |
| `background` | text part | 可选，可能为 `transparent` |

不要只接受 `image[]`。可以兼容 `image[]`，但必须同时接受目标客户端发送的重复 `image` 字段

目标客户端不再按比例或 K 档计算像素。LiteLLM 应在选部署前校验这两个参数，并按供应商真实规格转换；例如 ZexAPI `gpt-image2` 的 `16:9 + 2K` 转成 `size=2560x1440`。不能因编辑参数 schema 或 `drop_params` 丢掉已支持的比例和 K 档。LiteLLM 对其他调用方保留的精确 `size` 兼容输入，若与比例或 K 档同时出现，必须校验一致性。

响应格式与 `/v1/images/generations` 相同，支持 `data[].url` 或 `data[].b64_json`，并保留所有结果

ZexAPI 异步图片型号通过 LiteLLM 内部提交及轮询，对外仍返回最终 `data[].url`。它们可承接普通参考图编辑，但供应商未声明 mask、严格保真或透明输出能力；此类请求必须在上游提交前拒绝，不能因为有 `/images/edits` 外壳就宣传为完整局部编辑或透明分层模型。原图文件转换为 Data URL 时保留调用前的流位置，不裁切或重采样。

## 9. 视频生成

### 9.1 创建请求

```http
POST /v1/videos
Authorization: Bearer <API_KEY>
Content-Type: multipart/form-data; boundary=...
Idempotency-Key: <stable_request_id>
X-Client-Request-Id: <same_stable_request_id>
```

```text
model=gateway-video-pro
prompt=A slow camera move through a quiet forest
seconds=5
size=1280x720
input_reference=@reference.png
```

字段约定

| 字段 | 类型 | 约定 |
| --- | --- | --- |
| `model` | text part | 必须 |
| `prompt` | text part | 必须 |
| `seconds` | text part | 必须，十进制秒数字符串 |
| `size` | text part | 可选，`WIDTHxHEIGHT` |
| `input_reference` | file part | 可选，最多一份图片 |

纯文生视频不发送 `input_reference`。带参考图、首帧或图生视频时发送一份真实图片文件

### 9.2 创建幂等

强烈建议 LiteLLM 把 `Idempotency-Key` 作为创建幂等键，并在共享持久层保存“幂等键、请求指纹、视频任务 ID、创建响应”的对应关系

同一幂等键和同一请求重放时应返回原任务，不得再次调用供应商。相同幂等键对应不同 body 时应返回 409

`X-Client-Request-Id` 与 `Idempotency-Key` 当前值相同，可以作为日志关联字段，但不能代替服务端请求指纹校验

这是 Proxy 级能力，不是单个 Provider adapter 的小改动。多 worker 部署需要原子 claim、唯一约束、multipart 内容指纹、TTL 清理和重启后可恢复的共享存储

### 9.3 创建响应

推荐返回 202，也接受 200

```http
HTTP/1.1 202 Accepted
Content-Type: application/json
```

```json
{
  "id": "video_123",
  "object": "video",
  "status": "queued",
  "model": "gateway-video-pro"
}
```

`id` 必须稳定，并且可以直接用于后续状态和 content 路径。成功响应缺少任务 ID 会被目标客户端视为提交结果未知

### 9.4 查询状态

```http
GET /v1/videos/video_123
Authorization: Bearer <API_KEY>
Accept: application/json
```

排队中

```json
{
  "id": "video_123",
  "object": "video",
  "status": "queued"
}
```

处理中

```json
{
  "id": "video_123",
  "object": "video",
  "status": "in_progress",
  "progress": 42
}
```

完成

```json
{
  "id": "video_123",
  "object": "video",
  "status": "completed",
  "output_url": "/v1/videos/video_123/content"
}
```

失败

```json
{
  "id": "video_123",
  "object": "video",
  "status": "failed",
  "error": {
    "message": "供应商返回的可公开失败原因",
    "code": "provider_failed"
  }
}
```

规范状态只使用 `queued`、`in_progress`、`completed`、`failed` 和 `cancelled`

完成响应必须同时包含目标客户端可读取的媒体地址。目标客户端接受 `output_url`、`url`、`video_url`、`media_url`、`content_url` 和 `result_url` 等常见字段

推荐使用 LiteLLM 已有的 `output_url` 字段，并将它归一化为同源相对 content 路径

```json
{"id":"video_123","status":"completed","output_url":"/v1/videos/video_123/content"}
```

也可以直接返回目标客户端服务端无需额外供应商凭据即可下载的绝对公网或签名 URL。该 URL 必须在有效期内允许服务端完成下载，并且不能只在供应商内网可见

不要直接暴露需要供应商私钥或自定义 Header 才能下载的 URL。此类地址应保留在 LiteLLM 内部，由 content 接口使用供应商凭据下载

如果只返回 `status: completed` 而没有 `output_url`、`url`、`video_url` 等媒体字段，目标客户端会把任务判为失败

### 9.5 视频内容

只有完成响应返回 `/v1/videos/{id}/content` 之类的网关媒体地址时，本接口才是硬性要求。完成响应直接给出可下载绝对 URL 时，目标客户端可以不调用本接口

```http
GET /v1/videos/video_123/content
Authorization: Bearer <API_KEY>
```

成功响应

```http
HTTP/1.1 200 OK
Content-Type: video/mp4
Content-Disposition: inline; filename="video_123.mp4"
Content-Length: 1234567

<MP4 bytes>
```

允许使用 206 响应 Range 请求。推荐支持 `Accept-Ranges: bytes`

不得向目标客户端返回 301、302、303、307 或 308。LiteLLM 必须在内部完成供应商查询、签名 URL 获取、重定向跟随和下载，然后直接返回视频字节

`HEAD /v1/videos/{id}/content` 是推荐兼容项，不是目标客户端正常成功链的硬性要求。若实现 HEAD，应返回与 GET 一致的 Content-Type、Content-Length 和缓存相关头，不返回 body

## 10. LiteLLM 当前分支需要对齐的项目

本节是实现提示，不改变前述外部契约

### 10.1 `/v1/models`

当前模型列表主要返回 `id`、`owned_by` 和可选 `mode`。为了让目标客户端稳定识别能力，建议在模型列表输出层增加 `capability`

`capability` 不能只依赖 cost map 中可选的 `mode`。公开 alias 不在 cost map 时可能没有 mode，应优先读取 Router deployment、显式 model_info 或专用目录配置。一个 alias 的多个 deployment 能力不一致时，应拒绝发布或从该客户端目录中排除

目录还需要按当前 API Key 和生成能力过滤模型，不能仅给现有响应类型增加一个字段后继续发布 embedding、rerank 或其他非创作模型

推荐映射

| LiteLLM mode | capability |
| --- | --- |
| `chat`、`completion` | `text` |
| `image_generation` | `image` |
| `video_generation` | `video` |
| `audio_speech` | `audio` |

如果暂不修改类型和输出层，至少把公开 alias 命名为包含 `image` 或 `video` 的稳定 ID

### 10.2 图像 Provider

LiteLLM 的 `/v1/images/generations` 和 `/v1/images/edits` 入站路由已经存在，edits 入站也已接受重复 `image` 和兼容 `image[]`。剩余工作是为准备发布给该客户端的 alias 配置真实 Provider generations/edits 能力并归一化响应

只有需要覆盖完整参考图入口的 image alias 才要求同时通过 generations 和 edits。纯文生图 alias 可以单独发布，但管理员不得把它用于参考图、编辑或分层任务

如果某供应商只支持异步图像任务，Provider adapter 需要在 LiteLLM 内部轮询到完成，再对目标客户端同步返回 `data[]`

### 10.3 视频 Provider

LiteLLM 的创建、状态和 content GET 外层路由已经存在，但这不代表某个 Provider 的状态和下载闭环已经可用。每个 Provider 都必须实现并测试 create、status，以及“直接可下载绝对 URL”或“网关 content”中的一种结果交付方式

1. 接收并消费 multipart `input_reference`，不能静默丢弃
2. 创建返回稳定 LiteLLM video ID
3. status transformation 使用正确的 Provider 查询路径并解码原始上游任务 ID
4. 完成状态可以直接返回可下载绝对 URL，或对外返回 `output_url: /v1/videos/{litellm_id}/content`
5. 使用 content 路径时，必须可靠保留供应商真实结果定位信息，或在 content 请求时重新查询状态取得结果 URL
6. content handler 使用 Provider 凭据取得真实结果并直接返回 bytes
7. 客户端侧永远看不到供应商下载凭据或必须自行跟随的重定向

当前类型已有 `output_url`，不需要为本契约新增 `url` 字段

LiteLLM 托管 video ID 包含 Provider、deployment model_id 和原始上游任务 ID。统一路由层或每个 Provider transformation 必须可靠解码原始 ID，并覆盖原始 ID 含 `/`、`;` 或 padding 的测试

依赖 Router 当前 deployment 恢复凭据时，创建响应编码的 model_id 必须稳定，并在任务生命周期内仍可解析。若删除或替换模型配置后旧任务仍需下载，应使用独立共享任务存储保存 Provider、API base、凭据引用和真实结果定位，不能只依赖响应对象内存

当前 content GET 外层会把完整视频读入内存并固定包装为 `video/mp4`，还没有 HEAD、Range 透传或流式代理。它满足最小 MP4 响应外壳，但 Provider 仍必须先正确取得真实内容。大文件、非 MP4 和生产并发还需要单独做资源与 MIME 验证

### 10.4 当前媒体 Provider 分支的已知边界

当前分支中，部分 Provider 已支持图像同步归一化、图像 edits 或视频 multipart 参考图，但并非每个 Provider 同时具备三项能力

每个公开 alias 必须按真实能力配置。要把某个 alias 发布为该客户端的完整 image 模型，generations 和 edits 都应通过；要发布为完整 video 模型，纯文本和单图 `input_reference` 两条创建链都应通过。只支持子集的 alias 可以发布，但不得在管理员侧把它用于未支持的模式

### 10.5 视频创建幂等

当前视频路由没有完整的共享幂等记录。若要实现本契约推荐的安全幂等，应作为 Proxy 级持久化能力设计，而不是在每个 Provider transformation 中各写一份

## 11. 一致性验收

上线前至少完成以下自动化验收，不使用浏览器 mock

### 11.1 模型目录

1. 使用接入 API Key 调用 `/v1/models`
2. 返回本渠道实际提供的 text、image、video 模型；若目标是三类全覆盖，则三类至少各一个
3. 每个 ID 稳定且 capability 正确
4. 同一 API Key 后续可以调用返回的所有模型

### 11.2 文本

1. 普通非流 Chat 返回 `choices[].message.content`
2. `response_format: json_object` 返回合法 JSON 字符串
3. tools 请求返回标准 tool call 或合法 JSON fallback
4. `stream: true` 返回合法 SSE，或使用明确的可回退 HTTP 状态

### 11.3 图像

1. generations 返回至少一项 `data[].url` 或 `data[].b64_json`
2. URL 可由目标客户端服务端下载
3. 对声明支持参考图的 alias，edits 接受两个重复 `image` part 和一个可选 `mask`
4. 多结果不会被 LiteLLM 或 Provider adapter 截断

### 11.4 视频

1. multipart 纯文生视频创建成功
2. 对声明支持参考图的 alias，multipart 单 `input_reference` 创建成功
3. 若启用推荐幂等，相同幂等键重放仍返回同一 ID，供应商创建次数保持 1
4. 状态依次可返回 queued、in_progress、completed
5. completed 同时返回可下载绝对媒体 URL，或同源 content `output_url`
6. 使用同源 content 时，GET 直接返回 `video/mp4` 字节且没有 Location 头
7. 上游失败时状态为 failed，并带结构化 error.message
8. 整个轮询和下载过程中没有第二次创建 POST

## 12. 完成标准

只有目录实际发布的能力满足对应条件，才可以在管理员页面把渠道标记为可用。以下是文本、图像、视频三类全覆盖渠道的完成标准

- `/v1/models` 返回真实已配置模型，不是空列表或仅有 Swagger 路由
- 文本普通问答与结构化规划通过；流式返回合法 SSE，或明确拒绝后非流回退成功
- 完整图像 alias 的 generations 与 edits 都通过
- 完整视频 alias 的纯文本、单参考图、状态轮询和最终媒体下载都通过
- 若启用推荐视频幂等，幂等重放与冲突检查通过
- 所有媒体由目标客户端服务端真实下载并持久化
- 统一创作、图像工作台、视频工作台、Canvas 和短剧入口使用同一套公开模型 ID 完成真实业务回归

满足这些条件后，目标客户端只需要页面配置，不需要新增协议、Provider 适配或业务代码修改
