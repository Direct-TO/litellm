# 文本、图像与视频接口调用说明

## 基础信息

所有接口使用统一的服务地址和 Bearer Token 鉴权

```powershell
$serviceBaseUrl = "https://your-service.example.com"
$serviceApiKey = "<API_KEY>"
```

请求头：

```text
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

| 能力 | 方法 | 接口 |
| --- | --- | --- |
| 文本生成 | POST | `/v1/chat/completions` |
| 图像生成 | POST | `/v1/images/generations` |
| 视频生成 | POST | `/v1/videos` |
| 查询视频状态 | GET | `/v1/videos/{video_id}` |
| 下载视频 | GET | `/v1/videos/{video_id}/content` |

## 文本生成

请求：

```powershell
curl.exe -X POST "$serviceBaseUrl/v1/chat/completions" `
  -H "Authorization: Bearer $serviceApiKey" `
  -H "Content-Type: application/json" `
  --data-raw '{"messages":[{"role":"user","content":"请介绍一下杭州"}],"stream":false}'
```

响应示例：

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "杭州是浙江省省会……"
      }
    }
  ]
}
```

流式输出时，将请求中的 `stream` 设置为 `true`

## 图像生成

请求：

```powershell
curl.exe -X POST "$serviceBaseUrl/v1/images/generations" `
  -H "Authorization: Bearer $serviceApiKey" `
  -H "Content-Type: application/json" `
  --data-raw '{"prompt":"一只坐在窗边的橙猫，电影感光线","size":"1:1","response_format":"url"}'
```

响应示例：

```json
{
  "data": [
    {
      "url": "https://example.com/generated-image.png"
    }
  ]
}
```

从 `data[0].url` 获取生成后的图片

## 视频生成

创建视频：

```powershell
curl.exe -X POST "$serviceBaseUrl/v1/videos" `
  -H "Authorization: Bearer $serviceApiKey" `
  -H "Content-Type: application/json" `
  --data-raw '{"prompt":"日落时分，海浪缓慢拍打沙滩，电影感镜头","seconds":"12","size":"1920x1080"}'
```

创建成功后会返回视频任务 ID：

```json
{
  "id": "video_xxx",
  "status": "queued"
}
```

查询任务状态：

```powershell
$videoId = "video_xxx"

curl.exe -X GET "$serviceBaseUrl/v1/videos/$videoId" `
  -H "Authorization: Bearer $serviceApiKey"
```

常见状态：

- `queued`：等待处理
- `in_progress`：正在生成
- `completed`：生成完成
- `failed`：生成失败

任务完成后下载视频：

```powershell
curl.exe -L "$serviceBaseUrl/v1/videos/$videoId/content" `
  -H "Authorization: Bearer $serviceApiKey" `
  -o generated-video.mp4
```

## 错误处理

- HTTP 200：请求成功
- HTTP 400：请求参数错误
- HTTP 401 或 403：鉴权失败或无访问权限
- HTTP 429：请求频率过高
- HTTP 500 或 503：服务暂时不可用，可稍后重试
